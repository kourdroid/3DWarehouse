# ══════════════════════════════════════════════════════
#  SMATCH 3D WAREHOUSE — Backend Dependencies
#  Run: pip install -r requirements.txt
# ══════════════════════════════════════════════════════

# ── Core Framework ─────────────────────────────────────
fastapi==0.131.0
uvicorn==0.41.0
starlette==0.52.1
websockets==16.0

# ── Data Validation ────────────────────────────────────
pydantic==2.12.5
pydantic-settings==2.13.1

# ── Database (Async SQLite for local dev) ──────────────
SQLAlchemy==2.0.46
aiosqlite==0.22.1

# ── Excel Parsing (Layout Upload) ─────────────────────
lxml==6.0.2
python-docx==1.2.0

# ── Environment ────────────────────────────────────────
python-dotenv==1.2.1

# ── HTTP Client (for external WMS calls) ──────────────
httpx==0.28.1

# ── Redis (optional — only needed for Pub/Sub in prod)─
redis==7.2.1

# ── Testing ────────────────────────────────────────────
pytest==9.0.2
